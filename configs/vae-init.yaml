num_epochs: 5
log_interval: 100
clip_grad: 1.0

lr: 3e-4
weight_decay: 0.2

model:
  image_channels: 1
  latent_channels: 2

  encoder:
    dims: [64, 128, 256]
    depths: [2, 3, 3]

  decoder:
    dims: [ 64, 128, 256 ]
    depths: [ 2, 3, 3 ]

loss:
  kl_weight: 1e-4

dataset:
  batch_size: 32